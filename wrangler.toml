name = "autonomous-ai-agent"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# ─── Public vars (not sensitive) ──────────────────────────────────────────────
[vars]
# Your URLs
WORKER_URL = "https://autonomous-ai-agent.adesrnd.workers.dev"
UPSTASH_WORKFLOW_URL = "https://autonomous-ai-agent.adesrnd.workers.dev" # Must be worker for Upstash
BRIDGE_URL = "https://vega-ebon.vercel.app" # Next.js bridge (Vercel) for local-fs and other API routes
QSTASH_URL = "https://qstash-us-east-1.upstash.io" # Regional QStash; use same region as your Upstash project
# ─── D1 (Telegram configs per user) ─────────────────────────────────────────────
# 1. Create DB:  npx wrangler d1 create vega-d1
# 2. Copy the returned database_id into the line below (replace the zeros)
# 3. Apply schema:  npx wrangler d1 execute vega-d1 --remote --file=./migrations/0000_telegram_configs.sql
[[d1_databases]]
binding         = "DB"
database_name   = "vega-d1"
database_id     = "b72e0170-75aa-42c1-8194-7f4588d3dee1"

# ─── R2 File Storage ──────────────────────────────────────────────────────────
# Create bucket first: wrangler r2 bucket create vega-agent-files
[[r2_buckets]]
binding     = "FILES_BUCKET"
bucket_name = "vega-agent-files"

# ─── Secrets — set with: wrangler secret put SECRET_NAME ──────────────────────
# AI & Search
# GEMINI_API_KEY            → https://aistudio.google.com/apikey
# SERPER_API_KEY            → https://serper.dev (optional, DuckDuckGo used as fallback)

# Queue & Orchestration
# QSTASH_TOKEN              → https://console.upstash.com/qstash
# QSTASH_CURRENT_SIGNING_KEY
# QSTASH_NEXT_SIGNING_KEY

# Redis
# UPSTASH_REDIS_REST_URL    → https://console.upstash.com/redis
# UPSTASH_REDIS_REST_TOKEN

# Vector Memory
# UPSTASH_VECTOR_REST_URL   → https://console.upstash.com/vector
# UPSTASH_VECTOR_REST_TOKEN

# Code Execution
# E2B_API_KEY               → https://e2b.dev (for run_code tool)

# Integrations (all optional — tools degrade gracefully without them)
# GITHUB_TOKEN              → GitHub personal access token (repo scope)
# RESEND_API_KEY            → https://resend.com
# RESEND_FROM_EMAIL         → Your verified sender email
# TWILIO_ACCOUNT_SID        → https://console.twilio.com
# TWILIO_AUTH_TOKEN
# TWILIO_FROM_NUMBER        → E.164 format e.g. +1234567890
# BROWSERLESS_TOKEN         → https://browserless.io (for browse_web tool)